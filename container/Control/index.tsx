import { Wrapper, Button } from './styled';

const Control = () => {
  return <Wrapper>
		<Button>Prev</Button>
		<Button>Play</Button>
		<Button>Next</Button>
	</Wrapper>;
};

export default Control;
